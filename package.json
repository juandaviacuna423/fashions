{
  "name": "fashions-fullstack",
  "version": "1.0.0",
  "description": "FashionStore - E-commerce FullStack de Ropa",
  "scripts": {
    "start": "node backend/server.js",
    "build": "npm install --prefix backend && node backend/db/seed.js",
    "install-all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
